---
version: '3'
services:
  packer:
    build:
      context: ./packer
    dns_search: home.local
    dns:
      - 192.168.200.1
    volumes:
      - ./vimrc:/root/.vimrc
      - ./workspace:/packer
      - ./ssh:/keys
    entrypoint: bash /packer/start.sh
